<!DOCTYPE html>
<html lang="sk">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>AIR CHECK – Prihlásenie</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="icon" type="image/png" href="/images/favicon.ico">
  <style>
    body {
      background-image: url('/images/background.png'); 
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
  
    .form-container {
      background-color: rgba(255, 255, 255, 0.95);
      border-radius: 10px;
      padding: 2rem;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
  </style>
  
</head>
<body class="bg-light d-flex align-items-center" style="min-height: 100vh;">

  <div class="container">
    <div class="row justify-content-center">
      <div class="col-12 col-sm-8 col-md-6 col-lg-5">

        <h2 class="text-center mb-3">Vitaj v AIR CHECK</h2>
        <p class="text-center text-muted mb-4">
          Prihlás sa, sleduj kvalitu ovzdušia vo svojom okolí a získaj prístup k API tokenu a upozorneniam.
        </p>

        <div class="form-container">
        <% if (messages.error) { %>
          <div class="alert alert-danger">
            <% messages.error.forEach(function(msg) { %>
              <p class="mb-0"><%= msg %></p>
            <% }); %>
          </div>
        <% } %>

        <% if (messages.success_msg) { %>
          <div class="alert alert-success">
            <% messages.success_msg.forEach(function(msg) { %>
              <p class="mb-0"><%= msg %></p>
            <% }); %>
          </div>
        <% } %>

        <form action="/users/login" method="POST" class="bg-white p-4 rounded shadow-sm">
          <div class="mb-3">
            <label for="email" class="form-label">Email:</label>
            <input type="email" class="form-control" id="email" name="email" placeholder="Zadaj email" required>
          </div>

          <div class="mb-3">
            <label for="password" class="form-label">Heslo:</label>
            <input type="password" class="form-control" id="password" name="password" placeholder="Zadaj heslo" required>
          </div>

          <div class="d-grid gap-2">
            <button type="submit" class="btn btn-primary">Prihlásiť sa</button>
          </div>

          <div class="mt-3 text-center">
            <a href="/users/register">Nemáš účet? Zaregistruj sa tu</a>
          </div>
        </form>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
